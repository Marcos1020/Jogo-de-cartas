server:
  port: 8090
spring:
  datasource:
    url: jdbc:postgresql://localhost:5432/postgres
    username: cartas
    password: cartas
  jpa:
    database-platform: org.hibernate.dialect.PostgreSQLDialect
    database: postgresql
    open-in-view: false
    hibernate:
      ddl-auto: none
      naming:
        physical-strategy: org.hibernate.boot.model.naming.PhysicalNamingStrategyStandardImpl

feign:
  client:
    config:
      create-deck-game:
        url: https://deckofcardsapi.com/api/deck
      draw-card-initialazer-game:
        url: https://deckofcardsapi.com/api/deck

url-que-lista-decks-validos:
  url: "http://localhost:8090/v1/api/jogo-de-cartas/list/all"

url-que-gera-um-novo-deck:
  url: "http://localhost:8090/v1/api/jogo-de-cartas/"